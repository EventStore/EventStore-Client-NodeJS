{
  "name": "@kurrent/opentelemetry",
  "version": "1.0.0",
  "description": "Kurrent NodeJS Client SDK interceptors for tracing with OpenTelemetry",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm run build:ts",
    "build:ts": "tsc --build",
    "build:watch": "pnpm run build:ts-watch",
    "build:ts-watch": "tsc --build --watch",
    "clean": "pnpm run \"/^clean:.*/\"",
    "clean:dist": "shx rm -rf ./dist",
    "clean:buildinfo": "shx rm -rf *.tsbuildinfo"
  },
  "files": [
    "dist/"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EventStore/Kurrent-Client-NodeJS.git"
  },
  "keywords": [
    "EventStore",
    "EventSourcing",
    "OpenTelemetry"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": "^12.18.3 || >=14"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "@opentelemetry/instrumentation": "^0.56.0"
  },
  "devDependencies": {
    "@kurrent/db-client": "workspace:*"
  },
  "peerDependencies": {
    "@kurrent/db-client": "^1"
  }
}
