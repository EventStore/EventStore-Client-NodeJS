{
  "name": "benchmark",
  "packageManager": "yarn@4.6.0",
  "version": "1.0.0",
  "scripts": {
    "benchmark": "yarn benchmark:legacy-client && yarn benchmark:kurrentdb-client && yarn benchmark:bridge-client",
    "benchmark:legacy-client": "node ./legacy-client/readStream.js",
    "benchmark:kurrentdb-client": "node ./kurrentdb-client/readStream.js",
    "benchmark:bridge-client": "node ./bridge-client/readStream.js",
    "loadEvents": "node ./load"
  },
  "dependencies": {
    "@eventstore/db-client": "^6.2.1",
    "tinybench": "^3.1.1"
  },
  "devDependencies": {
    "@kurrent/bridge": "portal:../bridge",
    "@kurrent/kurrentdb-client": "portal:../db-client",
    "clinic": "^13.0.0"
  }
}
